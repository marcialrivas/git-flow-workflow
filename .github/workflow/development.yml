on:
    push:
        branches:
            - development
jobs:
    development:
        name: development
        runs-on: ubuntu-latest
        steps:
            - name: chekout
              uses: actions/chekout@v2
              with:
                fetch-depth: 0
            - name: build
              run: echo "Build"
            - name: unit test
              run: ehco "unit test"
            - name: static analysis
              run: echo "static analysis"